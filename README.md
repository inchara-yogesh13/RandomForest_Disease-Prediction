# RandomForest_Disease-Prediction
The Disease Prediction and Medicine Recommendation System uses a Random Forest Classifier to predict diseases based on user-entered symptoms. Trained on a healthcare dataset, it identifies the most likely disease and displays the corresponding medicine, demonstrating the practical use of machine learning in healthcare.
